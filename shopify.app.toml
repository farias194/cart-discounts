# Learn more about configuring your app at https://shopify.dev/docs/apps/tools/cli/configuration

client_id = "4e6c6360c1f8762d1b32a66e8d9c2a41"
name = "Cart Discounts"
# ⚠️ PLACEHOLDER — replaced automatically during `shopify app dev`.
# Set a real https URL before `shopify app deploy` (e.g. your hosted app URL).
application_url = "https://example.com"
embedded = true

[webhooks]
api_version = "2026-10"

# Discount webhooks (for syncing threshold messages)
[[webhooks.subscriptions]]
topics = [ "discounts/create" ]
uri = "/webhooks"

[[webhooks.subscriptions]]
topics = [ "discounts/update" ]
uri = "/webhooks"

[[webhooks.subscriptions]]
topics = [ "discounts/delete" ]
uri = "/webhooks"

# App lifecycle webhooks (preserved from the template)
[[webhooks.subscriptions]]
topics = [ "app/uninstalled" ]
uri = "/webhooks/app/uninstalled"

[[webhooks.subscriptions]]
topics = [ "app/scopes_update" ]
uri = "/webhooks/app/scopes_update"

[access_scopes]
# Learn more at https://shopify.dev/docs/apps/tools/cli/configuration#access_scopes
scopes = "read_discounts,write_metaobject_definitions,write_metaobjects,write_products"

[auth]
# ⚠️ PLACEHOLDER — replaced automatically during `shopify app dev`.
redirect_urls = [ "https://example.com/api/auth" ]

[build]
automatically_update_urls_on_dev = true
